# Set this in the Vercel dashboard: Settings -> Environment Variables.
# It is both the site password and the key that signs session cookies,
# so changing it logs every session out.
PORTAL_TOKEN=choose-a-long-random-string
